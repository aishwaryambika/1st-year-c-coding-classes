#include<stdio.h>
int main()
int sum=0;
        int product=1;
        while(n>0)
        {
            sum+=n%10;
            product*=n%10;
            n/=10;
        }
        product-sum;
    return(0);
    }
